# Structure_Sequence-Topt
A deep learning model for predicting the optimal growth temperature (Topt) of proteins by fusing sequence and structure embeddings via cross-attention.
# SeqStruct Topt Predictor

A deep learning model for predicting the optimal growth temperature (Topt) of proteins by fusing sequence and structure embeddings via cross-attention.

---

## Overview
